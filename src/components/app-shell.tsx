"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { motion } from "framer-motion";
import { PwaInstall } from "@/components/pwa-install";
import { CalendarDays, ChartNoAxesCombined, CheckSquare, Home, NotebookPen, Settings, Sparkles, Trophy, Terminal } from "lucide-react";
const links=[['/dashboard','Console',Home],['/habits','Processes',CheckSquare],['/calendar','Chronology',CalendarDays],['/progress','Telemetry',ChartNoAxesCombined],['/journal','Logbook',NotebookPen],['/insights','AI signals',Sparkles],['/achievements','Badges',Trophy],['/settings','Settings',Settings]] as const;
export function AppShell({children}:{children:React.ReactNode}){ const pathname=usePathname(); return <div className="terminal-grid min-h-screen md:flex"><aside className="glass-panel relative z-10 border-x-0 border-t-0 p-4 md:min-h-screen md:w-64 md:border-b-0 md:border-r"><Link href="/dashboard" className="mb-7 flex items-center gap-2 px-2 text-base font-bold uppercase tracking-[.14em] text-[#eafff5]"><Terminal size={18} className="text-[#58ffbd]"/>momentum<span className="text-[#4eeaff]">_os</span></Link><p className="terminal-label mb-2 px-3">system navigation</p><nav className="grid grid-cols-2 gap-1 sm:grid-cols-3 md:block">{links.map(([href,label,Icon],i)=><motion.div key={href} initial={{opacity:0,x:-8}} animate={{opacity:1,x:0}} transition={{delay:i*.035}}><Link className={`nav-link ${pathname===href?'nav-link-active':''}`} href={href as never}><Icon size={16}/><span>{label}</span></Link></motion.div>)}</nav><div className="mt-6 px-3"><PwaInstall/></div><div className="mt-5 hidden border-t border-[#54ffcd]/15 px-3 pt-4 md:block"><p className="terminal-label">connection</p><p className="mt-2 text-[11px] text-[#58ffbd]"><span className="mr-2 inline-block h-1.5 w-1.5 animate-pulse rounded-full bg-[#58ffbd]"/>ALL SYSTEMS READY</p></div></aside><main className="relative min-w-0 flex-1 p-5 sm:p-7 md:p-10"><div className="mx-auto max-w-6xl"><motion.div initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} transition={{duration:.35}}>{children}</motion.div></div></main></div> }
