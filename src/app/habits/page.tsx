import { AppShell } from "@/components/app-shell"; import { auth } from "@/auth"; import { prisma } from "@/lib/prisma"; import { ProcessRegistry } from "@/components/process-registry";
export const dynamic="force-dynamic";
export default async function Habits(){const s=await auth();const u=await prisma.user.findUniqueOrThrow({where:{email:s?.user?.email??""}});const habits=await prisma.habit.findMany({where:{userId:u.id},orderBy:{createdAt:"asc"}});return <AppShell><p className="terminal-label text-[#4eeaff]">/ process registry</p><h1 className="terminal-title mt-2 text-3xl"><span className="type-line">Habits & goals</span><span className="cursor"/></h1><ProcessRegistry processes={habits.map(h=>({id:h.id,title:h.title,archivedAt:h.archivedAt?.toISOString()??null}))}/></AppShell>}
